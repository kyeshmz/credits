Execute Phase 1 of the plan at `.plans/startup-credits-monorepo.md`.

## What this phase must achieve
At the end of this phase `pnpm install` succeeds from the repo root, and `@wholeearth/credits-data` typechecks and exports a typed, validated list of credit programs.

## Read this first
Open `.plans/startup-credits-monorepo.md`. The sections that apply to you: Conventions in force, Evidence, Existing code to reuse, Phase 1, Out of scope, Halt surfaces, Definition of done. Also read `~/.claude/skills/handoff-contract/SKILL.md` and `~/.claude/agents/implementer.md` — you are acting as the implementer role defined there and must report in that format.

## Files you own in this phase
- `package.json`
- `pnpm-workspace.yaml`
- `.npmrc`
- `.gitignore`
- `tsconfig.base.json`
- `packages/credits-data/package.json`
- `packages/credits-data/tsconfig.json`
- `packages/credits-data/src/types.ts`
- `packages/credits-data/src/credits.json`
- `packages/credits-data/src/index.ts`
- `pnpm-lock.yaml` (generated by `pnpm install`)
Do not create, edit, or delete any file outside this list, except the plan file, where you may tick your phase's `#### Automated verification` checkboxes and append to `## Execution log`.

## Already on disk from earlier phases
Nothing; this is the first phase. The repo contains only `.plans/` (the plan, this dispatch, and `.plans/models-dev-reference.css`) committed at f097d04.

## Conventions that apply here
- Package manager: pnpm 11 (`pnpm --version` -> 11.22.0). Node 22 (`node --version` -> v22.21.0).
- Install: `pnpm install`
- Typecheck (data pkg): `pnpm --filter @wholeearth/credits-data typecheck`
- Package names are scoped `@wholeearth/<dir-name>`. Apps live in `apps/*`, shared packages in `packages/*`.
- TypeScript strict. ESM only (`"type": "module"` everywhere).
- Forbidden here: `any` in TypeScript, committing `node_modules` or `dist`.
- Commits: do not commit; leave changes unstaged. The planner commits at the end.

## Verification you must run
- `pnpm install` — the workspace resolves and installs
- `pnpm --filter @wholeearth/credits-data typecheck` — the data package typechecks under strict TS
- `node --experimental-strip-types -e "import('./packages/credits-data/src/index.ts').then(m => { if (m.credits.length !== 18) throw new Error('expected 18, got ' + m.credits.length); console.log('ok', m.credits.length, m.categories.join(',')) })"` — the JSON validates at import time and has 18 entries

## Out of scope for this dispatch
- Anything under `apps/` (Phase 2 and 3).
- Provider logos, deployment config, live verification of program values.

Report in the format your role definition specifies. Your final message is all I will see, so make it complete on its own.
These instructions supersede any conflicting general instruction in your own role definition. Do only the work described here.
